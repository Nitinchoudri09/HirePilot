# ─────────────────────────────────────────────────────────────────────────────
# Prep2Hire — Production Requirements
# Python 3.12 | Django 5.x | Render + PostgreSQL + WhiteNoise + Gunicorn
# ─────────────────────────────────────────────────────────────────────────────

# Core Django
Django==5.1.7
gunicorn==21.2.0

# Static files
whitenoise==6.7.0

# Database
dj-database-url==2.2.0
psycopg2-binary==2.9.11

# Environment variables
python-dotenv==1.0.1

# Document parsing (resume analyzer)
docx2txt==0.9
PyPDF2==3.0.1

# ML / NLP (resume similarity scoring)
scikit-learn==1.5.2
numpy==2.1.3
scipy==1.13.1
sentence-transformers==2.7.0

# Image handling
Pillow==10.4.0

# HTTP requests (Supabase auth, job APIs)
requests==2.32.3

# Google OAuth support
google-auth==2.29.0
google-auth-oauthlib==1.2.1

# Payment gateway — Razorpay (Python 3.12 compatible; setuptools provides pkg_resources)
setuptools==75.6.0
wheel==0.43.0
razorpay==1.4.2